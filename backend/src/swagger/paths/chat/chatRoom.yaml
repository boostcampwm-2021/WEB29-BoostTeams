get:
  summary: 채팅방 정보 가져오기
  tags: [chat]
  parameters:
    $ref: '../../components/chat.yaml#/components/parameters/ChatRoomInfo'
  responses:
    200:
      description: 성공
      content:
        application/json:
          schema:
            $ref: '../../components/chat.yaml#/components/schemas/ChatRoomInfo'
    404:
      description: error
